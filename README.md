# creditcard-card-service

Credit Card Management - Card Issuance and Management Service

## Build & Run
```bash
mvn spring-boot:run
docker build -t creditcard-card-service .
docker run -p 8081:8080 creditcard-card-service
```

## API: http://localhost:8081/swagger-ui.html
